const SectionTitle = ({ text }) => {
  return (
    <div className="border-b border-grey-200 pb-5">
      <h1 className="capitalize text-3xl font-medium tracking-wider">{text}</h1>
    </div>
  );
};

export default SectionTitle;
